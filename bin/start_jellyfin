#!/usr/bin/env bash

set -x

JELLYFINDIR=$HOME"/Documents/self-hosted/Jellyfin/backup"
FFMPEG="/usr/lib/jellyfin-ffmpeg/ffmpeg"

exec /opt/jellyfin/dist/jellyfin \
  -d $JELLYFINDIR/data \
  -C ~/.cache/jellyfin \
  -c $JELLYFINDIR/config \
  -l ~/.log/jellyfin \
  --ffmpeg $FFMPEG \

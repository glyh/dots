#!/usr/bin/env bash

set -x

JELLYFINDIR=$HOME"/Documents/self-hosted/Jellyfin"
FFMPEG="/usr/lib/jellyfin-ffmpeg/ffmpeg"

exec /usr/lib/jellyfin/jellyfin \
  -d $JELLYFINDIR/data \
  -C ~/.cache/jellyfin \
  -c $JELLYFINDIR/config \
  -l ~/.log/jellyfin \
  --ffmpeg $FFMPEG
